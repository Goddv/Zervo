diff --git a/components/net/embedder.rs b/components/net/embedder.rs
index 9912a7e9b22..b03847d0ca9 100644
--- a/components/net/embedder.rs
+++ b/components/net/embedder.rs
@@ -10,6 +10,8 @@ use embedder_traits::{
     WebResourceResponseMsg,
 };
 use net_traits::CookieOperationId;
+use net_traits::NetworkError;
+use net_traits::request::RequestId;
 use servo_base::id::WebViewId;
 use servo_url::ServoUrl;
 use tokio::sync::mpsc::UnboundedSender as TokioSender;
@@ -39,4 +41,18 @@ pub enum NetToEmbedderMsg {
     EmbedderCookieOperationResponseWithCookies(CookieOperationId, Vec<Cookie<'static>>),
     /// Response to an asynchronous request from SiteDataManager.
     EmbedderCookieOperationResponse(CookieOperationId),
+    /// A navigation response the renderer cannot display inline. The embedder
+    /// decides whether to take it over (typically to save it to disk).
+    OfferUnsupportedResponse {
+        webview_id: WebViewId,
+        request_id: RequestId,
+        url: ServoUrl,
+        content_type: Option<mime::Mime>,
+        default_filename: String,
+        responder: TokioOneshotSender<bool>,
+    },
+    /// A chunk of a response the embedder took over.
+    FetchResponseChunk(WebViewId, RequestId, Vec<u8>),
+    /// The end of a response the embedder took over.
+    FetchResponseEof(WebViewId, RequestId, Result<(), NetworkError>),
 }
diff --git a/components/net/fetch/methods.rs b/components/net/fetch/methods.rs
index 14fb7cbad9f..382173b6381 100644
--- a/components/net/fetch/methods.rs
+++ b/components/net/fetch/methods.rs
@@ -11,6 +11,7 @@ use base64::engine::general_purpose;
 use content_security_policy as csp;
 use crossbeam_channel::Sender;
 use devtools_traits::DevtoolsControlMsg;
+use embedder_traits::GenericEmbedderProxy;
 use embedder_traits::resources::{self, Resource};
 use headers::{AccessControlExposeHeaders, ContentType, HeaderMapExt};
 use http::header::{self, HeaderMap, HeaderName, RANGE};
@@ -23,7 +24,10 @@ use net_traits::fetch::headers::{determine_nosniff, extract_mime_type_as_mime};
 use net_traits::filemanager_thread::{FileTokenCheck, RelativePos};
 use net_traits::http_status::HttpStatus;
 use net_traits::policy_container::{PolicyContainer, RequestPolicyContainer};
+use net_traits::is_content_disposition_attachment;
+use net_traits::mime_classifier::MimeClassifier;
 use net_traits::request::{
+    DownloadHandling,
     BodyChunkRequest, BodyChunkResponse, CredentialsMode, Destination, Initiator,
     InsecureRequestsPolicy, InternalRequest, Origin, ParserMetadata, RedirectMode, Referrer,
     Request, RequestBody, RequestId, RequestMode, ResponseTainting, is_cors_safelisted_method,
@@ -46,6 +50,7 @@ use tokio::sync::Mutex as TokioMutex;
 use tokio::sync::mpsc::{UnboundedReceiver as TokioReceiver, UnboundedSender as TokioSender};
 
 use crate::connector::CACertificates;
+use crate::embedder::NetToEmbedderMsg;
 use crate::devtools::{
     send_early_httprequest_to_devtools, send_response_to_devtools, send_security_info_to_devtools,
 };
@@ -111,6 +116,7 @@ pub struct FetchContext {
     pub ignore_certificate_errors: bool,
     pub preloaded_resources: SharedPreloadedResources,
     pub in_flight_keep_alive_records: SharedInflightKeepAliveRecords,
+    pub embedder_proxy: GenericEmbedderProxy<NetToEmbedderMsg>,
 }
 
 #[derive(Default)]
@@ -401,7 +407,7 @@ pub async fn main_fetch(
     fetch_params: &mut FetchParams,
     cache: &mut CorsCache,
     recursive_flag: bool,
-    target: Target<'_>,
+    mut target: Target<'_>,
     done_chan: &mut DoneChannel,
     context: &FetchContext,
 ) -> Response {
@@ -861,6 +867,39 @@ pub async fn main_fetch(
         target.process_request_body(request);
     }
 
+    // If this response is something the renderer cannot display — an explicit
+    // attachment, or a type the parser has no support for — offer it to the
+    // embedder, which may take over the body and save it. Servo keeps doing
+    // the transfer, so cookies, auth and redirects still apply.
+    // `MimeClassifier::get_media_type` is the parser's own notion of what it
+    // can render, so the two can never drift apart.
+    let mut new_target;
+    if let Some(handling) = request.download_handling {
+        let content_type = response
+            .metadata()
+            .ok()
+            .and_then(|metadata| metadata.metadata().content_type.clone())
+            .map(|content_type| Mime::from(content_type.0));
+        let unrenderable = content_type
+            .as_ref()
+            .is_none_or(|mime| MimeClassifier::get_media_type(mime).is_none());
+        let should_offer = match handling {
+            DownloadHandling::Always => true,
+            DownloadHandling::WhenUnrenderable => {
+                is_content_disposition_attachment(&response.headers) || unrenderable
+            },
+        };
+        if should_offer {
+            new_target = context
+                .state
+                .offer_unsupported_response(request, &response)
+                .await;
+            if let Some(new_target) = new_target.as_mut() {
+                target = &mut **new_target;
+            }
+        }
+    }
+
     // Step 22.
     target.process_response(request, &response);
     // Send Response to Devtools
diff --git a/components/net/http_loader.rs b/components/net/http_loader.rs
index f9d99d61551..f970d0d5edc 100644
--- a/components/net/http_loader.rs
+++ b/components/net/http_loader.rs
@@ -39,6 +39,7 @@ use ipc_channel::router::ROUTER;
 use log::{debug, error, info, log_enabled, warn};
 use malloc_size_of::{MallocSizeOf, MallocSizeOfOps};
 use net_traits::blob_url_store::UrlWithBlobClaim;
+use net_traits::extract_filename_from_content_disposition;
 use net_traits::fetch::headers::get_value_from_header_list;
 use net_traits::http_status::HttpStatus;
 use net_traits::policy_container::{EmbedderPolicyValue, RequestPolicyContainer};
@@ -170,6 +171,78 @@ impl HttpState {
             ));
         receiver.await.ok()?
     }
+
+    pub(crate) async fn offer_unsupported_response(&self, request: &Request, response: &Response) -> Option<Box<dyn net_traits::FetchTaskTarget + Send>> {
+        let Some(webview_id) = request.target_webview_id else {
+            return None;
+        };
+        let fallback_filename = response
+            .url()
+            .and_then(|url| url.path().rsplit_once('/'))
+            .map(|(_, name)| name)
+            .unwrap_or("")
+            .to_string();
+        // Filename precedence, as browsers implement it: a server-supplied
+        // Content-Disposition name wins, then the name the page suggested via
+        // `<a download="...">`, then the last path segment of the URL.
+        let default_filename = extract_filename_from_content_disposition(&response.headers)
+            .or_else(|| {
+                request
+                    .download_filename
+                    .clone()
+                    .filter(|filename| !filename.is_empty())
+            })
+            .unwrap_or(fallback_filename);
+        let (sender, receiver) = tokio::sync::oneshot::channel();
+        let offer = NetToEmbedderMsg::OfferUnsupportedResponse {
+            webview_id,
+            request_id: request.id.clone(),
+            url: response.url()?.clone(),
+            content_type: response.metadata().ok()?.metadata().content_type.as_ref().map(|mime| mime::Mime::from(mime.0.clone())),
+            default_filename,
+            responder: sender,
+        };
+        self.embedder_proxy.send(offer);
+        let embedder_accepts = receiver.await.ok()?;
+        if !embedder_accepts {
+            return None;
+        }
+        Some(Box::new(EmbedderFetchTarget {
+            embedder_proxy: self.embedder_proxy.clone(),
+        }))
+    }
+}
+
+struct EmbedderFetchTarget {
+    embedder_proxy: GenericEmbedderProxy<NetToEmbedderMsg>,
+}
+
+impl net_traits::FetchTaskTarget for EmbedderFetchTarget {
+    fn process_request_body(&mut self, _request: &Request) {}
+    fn process_response(&mut self, _request: &Request, _response: &Response) {}
+    fn process_response_chunk(&mut self, request: &Request, chunk: Vec<u8>) {
+        let Some(webview_id) = request.target_webview_id else {
+            return;
+        };
+        self.embedder_proxy.send(NetToEmbedderMsg::FetchResponseChunk(webview_id, request.id.clone(), chunk));
+    }
+    fn process_response_eof(&mut self, request: &Request, response: &Response) {
+        let Some(webview_id) = request.target_webview_id else {
+            return;
+        };
+        let result = match response.get_network_error() {
+            Some(error) => Err(error.clone()),
+            None => Ok(()),
+        };
+        self.embedder_proxy.send(NetToEmbedderMsg::FetchResponseEof(webview_id, request.id.clone(), result));
+    }
+    fn process_csp_violations(
+        &mut self,
+        _request: &Request,
+        _: Vec<content_security_policy::Violation>,
+    ) {
+    }
+    fn process_response_length_hint(&mut self, _request: &Request, _length: usize) {}
 }
 
 /// Step 11 of <https://fetch.spec.whatwg.org/#concept-fetch>.
diff --git a/components/net/resource_thread.rs b/components/net/resource_thread.rs
index 5be2947f542..c03eb8cf7f6 100644
--- a/components/net/resource_thread.rs
+++ b/components/net/resource_thread.rs
@@ -716,6 +716,7 @@ pub struct CoreResourceManager {
     preloaded_resources: SharedPreloadedResources,
     /// <https://fetch.spec.whatwg.org/#concept-fetch-record>
     in_flight_keep_alive_records: SharedInflightKeepAliveRecords,
+    embedder_proxy: GenericEmbedderProxy<NetToEmbedderMsg>,
 }
 
 impl CoreResourceManager {
@@ -731,11 +732,12 @@ impl CoreResourceManager {
             devtools_sender,
             sw_managers: Default::default(),
             filemanager: FileManager::new(embedder_proxy.clone(), blob_token_communicator),
-            request_interceptor: RequestInterceptor::new(embedder_proxy),
+            request_interceptor: RequestInterceptor::new(embedder_proxy.clone()),
             ca_certificates,
             ignore_certificate_errors,
             preloaded_resources: Default::default(),
             in_flight_keep_alive_records: Default::default(),
+            embedder_proxy,
         }
     }
 
@@ -832,6 +834,7 @@ impl CoreResourceManager {
             let entry = PreloadEntry::new(request.integrity_metadata.clone());
             preloaded_resources.insert(preload_id.clone(), entry);
         }
+        let embedder_proxy = self.embedder_proxy.clone();
 
         spawn_task(async move {
             // XXXManishearth: Check origin against pipeline id (also ensure that the mode is allowed)
@@ -853,6 +856,7 @@ impl CoreResourceManager {
                 ignore_certificate_errors,
                 preloaded_resources: preloaded_resources.clone(),
                 in_flight_keep_alive_records,
+                embedder_proxy,
             };
 
             match res_init_ {
@@ -916,6 +920,7 @@ impl CoreResourceManager {
         let ignore_certificate_errors = self.ignore_certificate_errors;
         let in_flight_keep_alive_records = self.in_flight_keep_alive_records.clone();
         let preloaded_resources = self.preloaded_resources.clone();
+        let embedder_proxy = self.embedder_proxy.clone();
 
         spawn_task(async move {
             let mut event_sender = event_sender;
@@ -952,6 +957,7 @@ impl CoreResourceManager {
                         ignore_certificate_errors,
                         preloaded_resources,
                         in_flight_keep_alive_records,
+                        embedder_proxy,
                     };
                     fetch(request, &mut event_sender, &context).await;
                 },
diff --git a/components/script/dom/html/embedded_content/htmlareaelement.rs b/components/script/dom/html/embedded_content/htmlareaelement.rs
index 8a857cb78e9..ccb5481343e 100644
--- a/components/script/dom/html/embedded_content/htmlareaelement.rs
+++ b/components/script/dom/html/embedded_content/htmlareaelement.rs
@@ -373,6 +373,12 @@ impl VirtualMethods for HTMLAreaElement {
 }
 
 impl HTMLAreaElementMethods<crate::DomTypeHolder> for HTMLAreaElement {
+    // https://html.spec.whatwg.org/multipage/#dom-a-download
+    make_getter!(Download, "download");
+
+    // https://html.spec.whatwg.org/multipage/#dom-a-download
+    make_setter!(SetDownload, "download");
+
     // https://html.spec.whatwg.org/multipage/#attr-hyperlink-target
     make_getter!(Target, "target");
 
diff --git a/components/script/dom/html/textual/htmlanchorelement.rs b/components/script/dom/html/textual/htmlanchorelement.rs
index fe5de38fa0a..e938b3f10ca 100644
--- a/components/script/dom/html/textual/htmlanchorelement.rs
+++ b/components/script/dom/html/textual/htmlanchorelement.rs
@@ -186,6 +186,12 @@ impl HTMLAnchorElementMethods<crate::DomTypeHolder> for HTMLAnchorElement {
     make_setter!(SetHreflang, "hreflang");
 
     // https://html.spec.whatwg.org/multipage/#dom-a-type
+    // https://html.spec.whatwg.org/multipage/#dom-a-download
+    make_getter!(Download, "download");
+
+    // https://html.spec.whatwg.org/multipage/#dom-a-download
+    make_setter!(SetDownload, "download");
+
     make_getter!(Type, "type");
 
     // https://html.spec.whatwg.org/multipage/#dom-a-type
@@ -379,8 +385,8 @@ impl Activatable for HTMLAnchorElement {
             ))
         }
 
-        // Step 2.
-        // TODO: Download the link is `download` attribute is set.
+        // Step 2: a `download` attribute makes this a download rather than a
+        // navigation; `follow_hyperlink` handles that.
         follow_hyperlink(cx, element, self.relations.get(), ismap_suffix);
     }
 }
diff --git a/components/script/links.rs b/components/script/links.rs
index 3ed1f60bbfb..95b1f09d99c 100644
--- a/components/script/links.rs
+++ b/components/script/links.rs
@@ -391,6 +391,13 @@ pub(crate) fn follow_hyperlink(
     relations: LinkRelations,
     hyperlink_suffix: Option<String>,
 ) {
+    // Step 2 of <https://html.spec.whatwg.org/multipage/#following-hyperlinks-2>:
+    // an `a`/`area` with a `download` attribute downloads the resource
+    // instead of navigating to it.
+    let download_attribute = (subject.is::<HTMLAnchorElement>() ||
+        subject.is::<HTMLAreaElement>())
+    .then(|| subject.get_attribute_string_value(&local_name!("download")))
+    .flatten();
     // Step 1: If subject cannot navigate, then return.
     if subject.cannot_navigate() {
         return;
@@ -483,8 +490,20 @@ pub(crate) fn follow_hyperlink(
         // Step 13: Navigate targetNavigable to urlString using subject's node document,
         //          with referrerPolicy set to referrerPolicy, userInvolvement set to
         //          userInvolvement, and sourceElement set to subject.
+        // <https://html.spec.whatwg.org/multipage/#downloading-hyperlinks>: the
+        // page may only suggest a filename for same-origin resources. For a
+        // cross-origin URL the download still happens, but the name comes from
+        // the response (Content-Disposition) or the URL instead.
+        let download = download_attribute.map(|filename| {
+            if document.origin().immutable().clone() == url.origin() {
+                filename
+            } else {
+                String::new()
+            }
+        });
+
         let secure = target_window.as_global_scope().is_secure_context();
-        let load_data = LoadData::new(
+        let mut load_data = LoadData::new(
             LoadOrigin::Script(document.origin().snapshot()),
             url,
             document.about_base_url(),
@@ -496,6 +515,7 @@ pub(crate) fn follow_hyperlink(
             document.has_trustworthy_ancestor_origin(),
             document.creation_sandboxing_flag_set_considering_parent_iframe(),
         );
+        load_data.download = download;
         let target = Trusted::new(target_window);
         let task = task!(navigate_follow_hyperlink: move |cx| {
             debug!("following hyperlink to {}", load_data.url);
diff --git a/components/script/navigation.rs b/components/script/navigation.rs
index 8159769f2b9..5d56b428ab0 100644
--- a/components/script/navigation.rs
+++ b/components/script/navigation.rs
@@ -16,6 +16,7 @@ use http::header;
 use js::context::JSContext;
 use net_traits::blob_url_store::UrlWithBlobClaim;
 use net_traits::request::{
+    DownloadHandling,
     CredentialsMode, InsecureRequestsPolicy, Origin, PreloadedResources, RedirectMode,
     RequestBuilder, RequestClient, RequestMode,
 };
@@ -253,7 +254,21 @@ impl InProgressLoad {
         .redirect_mode(RedirectMode::Manual)
         .crash(self.load_data.crash.clone())
         .client(request_client)
-        .url_list(self.url_list.clone());
+        .url_list(self.url_list.clone())
+        // A navigation renders what it can and hands anything else — an
+        // attachment, or a type the parser has no support for — to the
+        // embedder, rather than showing an "Unknown content type" page.
+        // A hyperlink with `download` skips rendering entirely.
+        .download_handling(match self.load_data.download {
+            Some(_) => DownloadHandling::Always,
+            None => DownloadHandling::WhenUnrenderable,
+        })
+        .download_filename(
+            self.load_data
+                .download
+                .clone()
+                .filter(|filename| !filename.is_empty()),
+        );
 
         request_builder.reload_navigation = self.load_data.reload_navigation;
         request_builder.history_navigation = self.load_data.history_navigation;
diff --git a/components/script_bindings/webidls/HTMLAnchorElement.webidl b/components/script_bindings/webidls/HTMLAnchorElement.webidl
index a6e52963d8f..d32b7421db9 100644
--- a/components/script_bindings/webidls/HTMLAnchorElement.webidl
+++ b/components/script_bindings/webidls/HTMLAnchorElement.webidl
@@ -17,8 +17,8 @@ interface HTMLAnchorElement : HTMLElement {
 
   [CEReactions]
   attribute DOMString target;
-  // [CEReactions]
-  //       attribute DOMString download;
+  [CEReactions]
+  attribute DOMString download;
   // [CEReactions]
   //       attribute USVString ping;
   [CEReactions]
diff --git a/components/script_bindings/webidls/HTMLAreaElement.webidl b/components/script_bindings/webidls/HTMLAreaElement.webidl
index 5e322024382..4e857328dff 100644
--- a/components/script_bindings/webidls/HTMLAreaElement.webidl
+++ b/components/script_bindings/webidls/HTMLAreaElement.webidl
@@ -15,8 +15,8 @@ interface HTMLAreaElement : HTMLElement {
   //         attribute DOMString shape;
   [CEReactions]
              attribute DOMString target;
-  // [CEReactions]
-  //         attribute DOMString download;
+  [CEReactions]
+  attribute DOMString download;
   // [CEReactions]
   //         attribute USVString ping;
   [CEReactions]
diff --git a/components/servo/Cargo.toml b/components/servo/Cargo.toml
index d9dce354a8d..ddf74f8dc22 100644
--- a/components/servo/Cargo.toml
+++ b/components/servo/Cargo.toml
@@ -128,6 +128,7 @@ layout = { workspace = true }
 layout_api = { workspace = true }
 log = { workspace = true }
 media = { workspace = true }
+mime = { workspace = true }
 mozangle = { workspace = true, optional = true }
 net = { workspace = true }
 net_traits = { workspace = true }
diff --git a/components/servo/lib.rs b/components/servo/lib.rs
index f7a33d88ca3..3769d4d5b97 100644
--- a/components/servo/lib.rs
+++ b/components/servo/lib.rs
@@ -71,6 +71,7 @@ pub use media::{
 };
 pub use net::image_cache::should_panic_hook_suppress_termination;
 pub use net_traits::CookieSource;
+pub use net_traits::request::{DownloadHandling, RequestId};
 // This API should probably not be exposed in this way. Instead there should be a fully
 // fleshed out public domains API if we want to expose it.
 pub use net_traits::pub_domains::is_reg_domain;
@@ -117,7 +118,7 @@ pub use crate::webview_delegate::{
     AlertDialog, AllowOrDenyRequest, AuthenticationRequest, BluetoothDeviceSelectionRequest,
     ColorPicker, ConfirmDialog, ContextMenu, CreateNewWebViewRequest, EmbedderControl, FilePicker,
     InputMethodControl, NavigationRequest, PermissionRequest, PromptDialog, SelectElement,
-    SimpleDialog, WebResourceLoad, WebViewDelegate,
+    SimpleDialog, UnsupportedResponse, WebResourceLoad, WebViewDelegate,
 };
 
 #[cfg(feature = "webxr")]
diff --git a/components/servo/responders.rs b/components/servo/responders.rs
index 4788323aaac..9f9b9b9b960 100644
--- a/components/servo/responders.rs
+++ b/components/servo/responders.rs
@@ -110,14 +110,14 @@ impl<T> AbstractSender for OneshotSender<T> {
 }
 
 /// Sends a response over an IPC channel, or a default response on [`Drop`] if no response was sent.
-pub(crate) struct IpcResponder<T> {
-    response_sender: Box<dyn AbstractSender<Message = T>>,
+pub(crate) struct IpcResponder<T: Send> {
+    response_sender: Box<dyn AbstractSender<Message = T> + Send>,
     response_sent: bool,
     /// Always present, except when taken by [`Drop`].
     default_response: Option<T>,
 }
 
-impl<T: Serialize + 'static> IpcResponder<T> {
+impl<T: Serialize + Send + 'static> IpcResponder<T> {
     pub(crate) fn new(response_sender: GenericSender<T>, default_response: T) -> Self {
         Self {
             response_sender: Box::new(response_sender),
@@ -127,9 +127,9 @@ impl<T: Serialize + 'static> IpcResponder<T> {
     }
 }
 
-impl<T: 'static> IpcResponder<T> {
+impl<T: Send + 'static> IpcResponder<T> {
     pub(crate) fn new_same_process(
-        response_sender: Box<dyn AbstractSender<Message = T>>,
+        response_sender: Box<dyn AbstractSender<Message = T> + Send>,
         default_response: T,
     ) -> Self {
         Self {
@@ -140,7 +140,7 @@ impl<T: 'static> IpcResponder<T> {
     }
 }
 
-impl<T> IpcResponder<T> {
+impl<T: Send> IpcResponder<T> {
     pub(crate) fn send(&mut self, response: T) -> SendResult {
         let result = self.response_sender.send(response);
         self.response_sent = true;
@@ -148,7 +148,7 @@ impl<T> IpcResponder<T> {
     }
 }
 
-impl<T> Drop for IpcResponder<T> {
+impl<T: Send> Drop for IpcResponder<T> {
     fn drop(&mut self) {
         if !self.response_sent {
             let response = self
diff --git a/components/servo/servo.rs b/components/servo/servo.rs
index 654e9d3f607..f32078bb546 100644
--- a/components/servo/servo.rs
+++ b/components/servo/servo.rs
@@ -442,6 +442,48 @@ impl ServoInner {
                 self.site_data_manager
                     .handle_cookie_response(operation_id, CookieOperationResponse::Done);
             },
+            NetToEmbedderMsg::OfferUnsupportedResponse {
+                webview_id,
+                request_id,
+                url,
+                content_type,
+                responder,
+                default_filename,
+            } => {
+                if let Some(webview) = self.get_webview_handle(webview_id) {
+                    let sender = crate::responders::OneshotSender::from(responder);
+                    let response = crate::webview_delegate::UnsupportedResponse {
+                        webview_id,
+                        request_id,
+                        url,
+                        content_type,
+                        default_filename,
+                        responder: crate::responders::IpcResponder::new_same_process(
+                            Box::new(sender),
+                            false,
+                        ),
+                    };
+                    webview
+                        .delegate()
+                        .notify_unsupported_response(webview, response);
+                }
+            },
+            NetToEmbedderMsg::FetchResponseChunk(webview_id, request_id, chunk) => {
+                if let Some(webview) = self.get_webview_handle(webview_id) {
+                    webview
+                        .delegate()
+                        .notify_response_chunk(webview, request_id, chunk);
+                }
+            },
+            NetToEmbedderMsg::FetchResponseEof(webview_id, request_id, result) => {
+                if let Some(webview) = self.get_webview_handle(webview_id) {
+                    webview.delegate().notify_response_eof(
+                        webview,
+                        request_id,
+                        result.map_err(|_| ()),
+                    );
+                }
+            },
         }
     }
 
diff --git a/components/servo/webview_delegate.rs b/components/servo/webview_delegate.rs
index 3bbdc5189ac..4818c6afd57 100644
--- a/components/servo/webview_delegate.rs
+++ b/components/servo/webview_delegate.rs
@@ -1088,6 +1088,48 @@ pub trait WebViewDelegate {
         _tree_update: accesskit::TreeUpdate,
     ) {
     }
+
+    ///
+    fn notify_unsupported_response(
+        &self,
+        _webview: WebView,
+        _response: UnsupportedResponse) {}
+    fn notify_response_chunk(
+        &self,
+        _webview: WebView,
+        _request_id: net_traits::request::RequestId,
+        _chunk: Vec<u8>) {}
+    fn notify_response_eof(
+        &self,
+        _webview: WebView,
+        _request_id: net_traits::request::RequestId,
+        _result: Result<(), ()>,
+    ) {}
+}
+
+pub struct UnsupportedResponse {
+    pub webview_id: servo_base::id::WebViewId,
+    pub request_id: net_traits::request::RequestId,
+    pub url: servo_url::ServoUrl,
+    pub content_type: Option<mime::Mime>,
+    pub default_filename: String,
+    pub(crate) responder: crate::responders::IpcResponder<bool>,
+}
+
+impl std::fmt::Debug for UnsupportedResponse {
+    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> Result<(), std::fmt::Error> {
+        f.write_str("UnsupportedResponse(..)")
+    }
+}
+
+impl UnsupportedResponse {
+    pub fn accept(&mut self) {
+        let _ = self.responder.send(true);
+    }
+
+    pub fn decline(&mut self) {
+        let _ = self.responder.send(false);
+    }
 }
 
 pub(crate) struct DefaultWebViewDelegate;
diff --git a/components/shared/constellation/from_script_message.rs b/components/shared/constellation/from_script_message.rs
index 37d9fe0a117..dcf89093f19 100644
--- a/components/shared/constellation/from_script_message.rs
+++ b/components/shared/constellation/from_script_message.rs
@@ -146,6 +146,13 @@ pub struct LoadData {
 
     /// If this request is for the initial about:blank document.
     pub is_initial_about_blank: bool,
+
+    /// Set when this navigation came from a hyperlink with a `download`
+    /// attribute (or an explicit "Save Link As"): the response is handed to
+    /// the embedder to save rather than rendered. The `String` is the
+    /// filename the page suggested, empty when it gave none.
+    /// <https://html.spec.whatwg.org/multipage/#downloading-hyperlinks>
+    pub download: Option<String>,
 }
 
 impl LoadData {
@@ -186,6 +193,7 @@ impl LoadData {
             creation_sandboxing_flag_set,
             container_document_encoding: None,
             is_initial_about_blank: false,
+            download: None,
         }
     }
 
diff --git a/components/shared/net/lib.rs b/components/shared/net/lib.rs
index ef3c043e4bf..582669b75ea 100644
--- a/components/shared/net/lib.rs
+++ b/components/shared/net/lib.rs
@@ -13,6 +13,7 @@ use cookie::Cookie;
 use crossbeam_channel::{Receiver, Sender, unbounded};
 use headers::{ContentType, HeaderMapExt, ReferrerPolicy as ReferrerPolicyHeader};
 use http::{HeaderMap, HeaderValue, StatusCode, header};
+use http::header::CONTENT_DISPOSITION;
 use hyper_serde::Serde;
 use hyper_util::client::legacy::Error as HyperError;
 use ipc_channel::ipc::{self, IpcSender};
@@ -1343,3 +1344,42 @@ pub fn set_default_accept_language(headers: &mut HeaderMap) {
 }
 
 pub static PRIVILEGED_SECRET: LazyLock<u32> = LazyLock::new(|| rng().next_u32());
+
+/// True when `Content-Disposition` marks the response as an attachment, i.e.
+/// the server asked for it to be saved rather than displayed.
+/// <https://httpwg.org/specs/rfc6266.html#disposition.type>
+pub fn is_content_disposition_attachment(headers: &HeaderMap) -> bool {
+    headers
+        .get(CONTENT_DISPOSITION)
+        .and_then(|value| value.to_str().ok())
+        .is_some_and(|value| {
+            value
+                .split(';')
+                .next()
+                .map(str::trim)
+                .is_some_and(|disposition| disposition.eq_ignore_ascii_case("attachment"))
+        })
+}
+
+pub fn extract_filename_from_content_disposition(headers: &HeaderMap) -> Option<String> {
+    let cd = headers.get(CONTENT_DISPOSITION)?.to_str().ok()?;
+    if let Some(index) = cd.find("filename=") {
+        let start = index + "filename=".len();
+        return Some(
+            cd.get(start..)
+                .unwrap_or_default()
+                .trim_matches('"')
+                .to_owned(),
+        );
+    }
+    if let Some(index) = cd.find("filename*=UTF-8''") {
+        let start = index + "filename*=UTF-8''".len();
+        return Some(
+            cd.get(start..)
+                .unwrap_or_default()
+                .trim_matches('"')
+                .to_owned(),
+        );
+    }
+    None
+}
diff --git a/components/shared/net/request.rs b/components/shared/net/request.rs
index 66c8817f8ed..5bf4f348707 100644
--- a/components/shared/net/request.rs
+++ b/components/shared/net/request.rs
@@ -427,6 +427,18 @@ impl RequestHeadersSize for HeaderMap {
     }
 }
 
+/// How a response should be handled when the renderer cannot display it.
+#[derive(Clone, Copy, Debug, Deserialize, Eq, MallocSizeOf, PartialEq, Serialize)]
+pub enum DownloadHandling {
+    /// Offer the response to the embedder when it is marked as an attachment
+    /// or is of a type the parser cannot render — the behaviour of an
+    /// ordinary navigation.
+    WhenUnrenderable,
+    /// Always offer the response to the embedder, whatever its type. Used by
+    /// `<a download>` and "Save Link As".
+    Always,
+}
+
 #[derive(Clone, Debug, Deserialize, MallocSizeOf, Serialize)]
 pub struct RequestBuilder {
     pub id: RequestId,
@@ -519,6 +531,13 @@ pub struct RequestBuilder {
     pub crash: Option<String>,
     /// Servo internal: whether this request originates from Servo internal implementation
     pub is_internal_request: InternalRequest,
+    /// Set for navigations, so a response this engine cannot render is handed
+    /// to the embedder (typically to be saved) instead of rendering an
+    /// "Unknown content type" page. `None` for subresources, which are never
+    /// offered to the embedder.
+    pub download_handling: Option<DownloadHandling>,
+    /// Filename proposed by the page, i.e. the value of `<a download="...">`.
+    pub download_filename: Option<String>,
 }
 
 impl RequestBuilder {
@@ -562,9 +581,21 @@ impl RequestBuilder {
             response_tainting: ResponseTainting::Basic,
             is_internal_request: Default::default(),
             crash: None,
+            download_handling: None,
+            download_filename: None,
         }
     }
 
+    pub fn download_handling(mut self, handling: DownloadHandling) -> RequestBuilder {
+        self.download_handling = Some(handling);
+        self
+    }
+
+    pub fn download_filename(mut self, filename: Option<String>) -> RequestBuilder {
+        self.download_filename = filename;
+        self
+    }
+
     pub fn preload_id(mut self, preload_id: PreloadId) -> RequestBuilder {
         self.preload_id = Some(preload_id);
         self
@@ -774,6 +805,8 @@ impl RequestBuilder {
         request.client = self.client;
         request.policy_container = self.policy_container;
         request.is_internal_request = self.is_internal_request;
+        request.download_handling = self.download_handling;
+        request.download_filename = self.download_filename.clone();
         request
     }
 
@@ -862,6 +895,13 @@ pub struct Request {
     pub crash: Option<String>,
     /// Servo internal: whether this request originates from Servo internal implementation
     pub is_internal_request: InternalRequest,
+    /// Set for navigations, so a response this engine cannot render is handed
+    /// to the embedder (typically to be saved) instead of rendering an
+    /// "Unknown content type" page. `None` for subresources, which are never
+    /// offered to the embedder.
+    pub download_handling: Option<DownloadHandling>,
+    /// Filename proposed by the page, i.e. the value of `<a download="...">`.
+    pub download_filename: Option<String>,
 }
 
 impl Request {
@@ -910,6 +950,8 @@ impl Request {
             policy_container: RequestPolicyContainer::Client,
             is_internal_request: Default::default(),
             crash: None,
+            download_handling: None,
+            download_filename: None,
         }
     }
 
